# Item Delete

The next piece of the CRUD pie is the delete, which is pretty simple.

Let's run the following:

```text
/feature load Create the delete functionality for items. There should be a Shadcn confirmation and a toast on success.
```

Run through the standard workflow and iterate where needed.
