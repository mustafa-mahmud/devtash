# User Profile

I want to have a profile link that just shows the user info, change password and allows them to delete their account if they want.

Let's create a new file at `context/features/profile-spec.md`. You can find the full file in the course files.

Then load it and run:

```test
/feature load profile-spec.md
/feature start
```

Once that is done, check the profile and test the reset password option.

If it is working, finish up the feature:

```text
/feature review
/feature/complete
/clear
```
