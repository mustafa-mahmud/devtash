# Small UI Update

I want to also add the same nav that is on the homepage to the sign in and sign up pages. I also want to update the dashboard logo to use the same folder icon as the homepage nav.

I ran this prompt:

```text
/feature load add the homepage top nav to the sign in and sign up pages. Then add the same folder icon that the homepage nav has for the logo in the dashboard nav
```

This will create the feature. Now you can run and go through the workflow:

```
/feature start
/feature complete
/clear
```