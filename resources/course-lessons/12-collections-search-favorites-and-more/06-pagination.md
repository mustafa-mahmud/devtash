# Pagination

Now we need some pagination. Copy the course file `context/features/pagination-spec.md` to your project and add the following prompt:

```
/feature load pagination-spec.md
```

Then run `/feature start`

Once done iterating, run:

```text
/feature review
/feature complete
/clear
```